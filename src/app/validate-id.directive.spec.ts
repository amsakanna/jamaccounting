import { ValidateIdDirective } from './validate-id.directive';

describe('ValidateIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateIdDirective();
    expect(directive).toBeTruthy();
  });
});
